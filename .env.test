# Test Environment Configuration
NODE_ENV=test
PORT=3001
API_VERSION=v1

# Database Configuration (Test Database)
DB_HOST=54.250.29.129
DB_PORT=5432
DB_NAME=648_test
DB_USER=postgres
DB_PASSWORD=123456
DB_MAX_CONNECTIONS=10

# JWT Configuration
JWT_SECRET=test-super-secret-jwt-key-for-testing-only
JWT_EXPIRES_IN=1h
JWT_REFRESH_SECRET=test-refresh-token-secret
JWT_REFRESH_EXPIRES_IN=7d

# Database Performance Configuration
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_IDLE_TIMEOUT=30000
DB_CONNECTION_TIMEOUT=5000
DB_STATEMENT_TIMEOUT=10000

# Rate Limiting (More lenient for testing)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=1000

# Logging (Minimal for tests)
LOG_LEVEL=error
LOG_FILE=logs/test.log
ERROR_LOG_FILE=logs/test-error.log
AUDIT_LOG_FILE=logs/test-audit.log

# Brand Settings
DEFAULT_BRAND_SLUG=test-brand
MAX_DAILY_SPINS=10
DEFAULT_MEMBER_POINTS=0

# Test-specific settings
VERBOSE_TESTS=false
TEST_TIMEOUT=30000